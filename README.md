This project is a compact, microcontroller-driven electronic lock that uses a 4x4 matrix keypad to control an SG90 micro servo deadbolt.
The system scansinputs and prompts user updates on a 16x2 character display.
It unlocks the mechanism only when a matching four-digit code is authenticated.
